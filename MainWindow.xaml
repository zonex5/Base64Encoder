<Window x:Class="Base64Encoder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Base64Encoder"
        mc:Ignorable="d" Height="320" Width="550"
        ResizeMode="NoResize" WindowStartupLocation="CenterScreen"
        Title="Base64 Encoder">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="WhiteSmoke" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="DimGray" />
            <Setter Property="Padding" Value="7 3" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="WhiteSmoke" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="LightGray" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="15 5" LastChildFill="False" VerticalAlignment="Bottom">
            <Button DockPanel.Dock="Left" Background="Transparent" Padding="3" ToolTip="Save to File" Click="ButtonSave_OnClick">
                <Image Opacity="0.6" Source="assets/save24.png" Width="16" Height="16" />
            </Button>
            <Button DockPanel.Dock="Left" Background="Transparent" Padding="3" ToolTip="Copy to Clipboard" Click="ButtonCopy_OnClick">
                <Image Opacity="0.5" Source="assets/copy24.png" Width="16" Height="16" />
            </Button>
            <Button x:Name="BtEncode" Click="ButtonBase_OnClick" Content="ASCII" DockPanel.Dock="Right" />
        </DockPanel>
        <TextBox x:Name="TbText" Grid.Row="1" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" AcceptsReturn="True" Margin="15 0 15 15" />
    </Grid>
</Window> 